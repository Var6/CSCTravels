/* Full-screen app shell — no site navbar, no footer.
   The booking page manages its own internal layout. */
export default function AppLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
